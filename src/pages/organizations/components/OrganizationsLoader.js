import React from 'react';

import Loader from '../../../components/Loader';

const OrganizationsLoader = () => (
  <Loader>Loading organizations data...</Loader>
);

export default OrganizationsLoader;
